import "reflect-metadata";
export default class ParserDecorate {
    static parser(map: Map<string, Function>): void;
    static parserAutowrite(): void;
    private static parseService;
    private static parseController;
}
