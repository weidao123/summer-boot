export default class Loader {
    static getPathAsExtname(p: string): string;
    static loadFile(path: string): any;
    static existFile(path: string): boolean;
    static load(path: string): Map<string, Function>;
}
